# 🛡️ Ficha de Cumplimiento ISO/IEC 27001:2022

## 1. Tabla de Controles del Anexo A

| Control | Cómo lo cubrimos | Evidencia | Cobertura | Brecha residual |
| :--- | :--- | :--- | :--- | :--- |
| **A.8.9**<br>Gestión de la configuración | Las 14 políticas de Gatekeeper obligan a que todo nuevo despliegue nazca con una configuración segura (ej. sin root). | Archivos `ConstraintTemplates` y `Constraints` versionados en Git (`manifests/`). | Parcial | Gatekeeper no tiene visibilidad alguna sobre el sistema operativo de los nodos (ej. Rocky Linux). |
| **A.5.36**<br>Cumplimiento de políticas y normas | El modo de auditoría (Dryrun) escanea el clúster cada 60s para detectar desviaciones en recursos ya existentes. | Dashboard GPM mostrando pods reportados en la pestaña "Violations" (`http://<IP>:30080`). | Parcial | Solo escanea el plano de contenedores en Kubernetes, no evalúa configuraciones del host. |
| **A.8.32**<br>Gestión de cambios | El Webhook de admisión intercepta la creación/modificación de recursos; si el cambio viola una regla de seguridad, se rechaza. | Ejecución de `scripts/test.sh` mostrando el rechazo de los pods con políticas incompletas. | Parcial | Los pods creados antes de instalar Gatekeeper no pasaron por este procedimiento de control. |
| **A.8.27**<br>Principios de arquitectura e ingeniería segura | Las políticas imponen el uso de `readOnlyRootFilesystem` y prohíben montar volúmenes del host (`hostPath`). | Manifiestos de restricción de volúmenes aplicados y demostración de bloqueo. | Parcial | La arquitectura segura se limita a contenedores, sin cubrir seguridad física o segmentación externa. |
| **A.8.34**<br>Protección de sistemas durante pruebas de auditoría | Las pruebas de cumplimiento (auditoría) se realizan mediante el modo `dryrun` y un dashboard de solo lectura, evitando alteraciones o caídas de los servicios en producción. | Aplicación de `pod-auditoria.yaml` que es reportado silenciosamente sin ser destruido ni afectar la disponibilidad. | Parcial | La protección durante la auditoría aplica solo a los recursos de K8s, no abarca pruebas de penetración a nivel red/nodo. |
| **A.8.2**<br>Derechos de acceso privilegiado | Las políticas que bloquean contenedores privilegiados, `hostPath` y escalada de privilegios. | Intento de crear un pod privilegiado, con el mensaje de rechazo. | Parcial | Valida lo que entra, no lo que ya está corriendo; no destruye pods privilegiados preexistentes. |

## 2. Brecha Residual y Auditoría

### 2.1 Qué NO queda cubierto por nuestra herramienta (La Brecha)
OPA Gatekeeper tiene una limitación técnica por diseño: **valida lo que entra, no lo que ya está corriendo**. Los pods creados antes de instalar Gatekeeper siguen ejecutándose; el motor solo los etiqueta en modo "audit", pero no los destruye. Además, Gatekeeper no tiene visibilidad alguna sobre el sistema operativo de los nodos (ej. Rocky Linux), por lo que no protege contra vulnerabilidades a nivel de host.

### 2.2 Qué haría falta para cerrar la brecha
*   **Para los pods en ejecución:** Se requeriría implementar una herramienta de seguridad en tiempo de ejecución (Runtime Security), como **Falco**, capaz de monitorear llamadas al sistema (syscalls) y matar contenedores maliciosos activos.
*   **Para el sistema operativo:** Implementar herramientas de escaneo tipo CIS Benchmarks (ej. `kube-bench`) y un proceso de gestión de parches (Patch Management) directo en los nodos.

### 2.3 Riesgos aceptados con justificación
Aceptamos conscientemente el riesgo de que recursos heredados con configuraciones inseguras continúen operando debido a que en un entorno de producción, forzar a Gatekeeper a matar pods preexistentes violaría el principio de Disponibilidad. Es preferible mantener el servicio arriba, detectar la brecha vía el Dashboard, y planificar un reemplazo ordenado de los pods vulnerables.
